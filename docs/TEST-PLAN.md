# Test Plan

> **SUPERSEDED IN PART (2026-07-15).** This plan predates Model B. It is authoritative only where it does not conflict with [ADR-0012](adr/0012-model-b-spend-and-duress-architecture.md) (architecture + duress), [ADR-0013](adr/0013-concrete-protocol-schemas.md) (concrete schemas), and [V0-PLAN.md](V0-PLAN.md) — **those win on every conflict.** The body below is left as-is; these items are known-superseded:
> - **Assembly + broadcast:** the coordinator-assembles-and-broadcasts flow is retired — under **Model B the nodes assemble partials over the Node channel and each broadcasts**; the coordinator is a pure relay that never combines, finalizes, or broadcasts (ADR-0012).
> - **`/sign` contract:** the `{psbt, escape_psbt, pin}` body is replaced by the **coordinator-signed tagged request schema** — `SpendRequest{spend, escape, escape_bumps, pin, nonce, expiry, policy_version}` plus a pin-less `RefreshRequest`, escape mandatory, optional pre-signed fee ladder, coord-signed and fresh (ADR-0013 §2).
> - **Watchtower criterion:** "a vault spend a node never **co-signed** → alert" is the wrong test (it false-alarms on the n−t legitimate non-signers) — the criterion is now "never **validated** (saw and policy-checked the request)" (ADR-0012).
> - **Retired milestones:** the v0.1 / v0.2 milestone split (and the "v0.2 semantics" anti-replay framing) is retired — v0 is the full core; see V0-PLAN.md.
> - **Attack scenarios (2026-07-21):** the seven-scenario v1 set under *Critical Paths* (stolen-user-key, compromised-node, mutation, replay, stolen-recovery-keys, compromised-quorum, recovery-drill) is retired. It predates the two-track duress state machine and framed safety as a receipt count; the 2026-07-20 dual-model review settled the property on the **coupling + release-gate** instead (ADR-0012). It is replaced by the adversarial regtest harness described below.

Generated by /plan-eng-review on 2026-07-13
Branch: main (header kept from the pre-code review; the repo was initialized 2026-07-14)
Repo: btc-policy (local)

## Affected Pages/Routes
- CLI: `btc-vault demo theft-refused` — the v0 acceptance flow: fund regtest vault, honest spend confirms (through the Hold, time-warped), act one: theft refused by all nodes with structured reason, act two: allowlisted theft caught mid-Hold and clawed back by escape sweep; meaningful exit code. (Milestones per design doc: v0 = the full core incl. Hold/PINs/duress/watchtower — the former v0.1/v0.2 split is retired; v1 = deployer/sealing/backend enforcement.)
- HTTP: `POST /sign` (two-phase: `{psbt, escape_psbt, pin}` → pending | signed | refusal), `GET /events?since=` (watchtower alerts and channel freshness rejects ONLY — pending spends moved to `/pending`, liveness to `/healthz`, and the sign-log was never built; corrected 2026-09-09), `POST /channel` (authenticated node-to-node envelopes, channel mode only), `GET /healthz` (read-only liveness: `{serving, locked_down, last_deadline_tick, generation_claimed}`, pin-uniform), and `GET /pending` (read-only accepted-but-unsettled spend projection: `{pending: [commitment_id, ...]}`, pin-uniform) on each vault-node; all adversarial input lands here, `/healthz` and `/pending` included — both are reachable over the same coordinator relay path.

## Key Interactions to Verify
- Honest spend end-to-end on regtest: build hot tx + escape variant → user signs both → PIN → one submission fans out to 5 nodes and each signs at ingress → holder decision opens the release gate → nodes release/combine/broadcast at Hold expiry without coordinator re-submission → confirm.
- Hold semantics: hot-class partial is signed but held at first submission; before Hold expiry it is not released, and after Hold expiry a holder-confirmed node releases it for combine+broadcast without re-submission. Escape and refresh classes have no Hold delay.
- Duress: duress PIN → wire response byte-identical to normal pending; configured duress_response executes (escape broadcast and/or lockdown); lockdown holds for the node's lifetime, and a rebooted node is DEAD — bare machine requiring full re-provisioning, never a node that comes back locked-down (RAMDISK deployment, ADR-0007; reboot-death/tmpfs, 2026-07-16); all subsequent submissions → FRAUD_SUSPECTED.
- Escape-sweep cancel: escape sweep confirms → conflicting pending spend can never complete (inputs gone); node prunes it.
- PIN gate: missing/wrong PIN → BAD_PIN, nothing signed, ever.
- Log reconciliation: a sign event pulled from a node that the coordinator didn't originate → user-facing alert.
- Watchtower: recovery-path spend on regtest → every node queues an alert; vault spend a node never co-signed → alert; GET /events cursor loses nothing, duplicates nothing.
- Theft attempt (attacker holds user key, non-allowlisted destination): every node refuses; refusal reason names the failing check.
- Mixed outcome: quorum reached with one refusal → transaction broadcasts AND a structured alert is emitted naming the refusing node and reason (decided D7/4A).
- Node availability: 2 nodes down on 3-of-5 → spend still succeeds; 3 nodes down → clean failure with a clear insufficient-quorum message, no hang.
- Demo idempotency: re-running `btc-vault demo` on dirty state (existing containers/wallet) either cleans up or fails with a clear message — never a half-broken federation.
- Setup validation: vault setup without an escape-cold-wallet allowlist entry fails with a clear message (mandatory per Wallet Topology); duress PIN equal or typo-adjacent to normal PIN → rejected at enrollment; escape descriptor sharing a BIP32 fingerprint/xpub/pubkey with the user key → hard error (ADR-0003 tripwire). **Extended 2026-07-25 (bead btc-policy-9y5.5), now that the ceremony is real (`btc-vault setup`, `docs/SETUP-CEREMONY.md`):** the escape wallet's DERIVED keys are scanned over `0..=max_derivation_index` against the user key, every node key, and every recovery key, and any overlap is a ceremony REFUSAL — the check with teeth now that vault keys are definite and carry no origin to compare. Covered by `setup::tests` (`a_shared_seed_escape_is_refused_at_ceremony_time`, `an_escape_sharing_a_node_or_recovery_key_is_refused`, `an_escape_sharing_an_origin_with_the_hot_wallet_is_refused`). Also ceremony-refused there rather than at node startup: a federation shape other than `n = 2t − 1`, a ranged vault key, a non-loopback v0 endpoint, and an endorsement that does not verify.

## Edge Cases
- Change output labeled as change but NOT derivable from the vault descriptor → refused (theft vector; policy-core unit test, both branches).
- Every policy check, pass AND fail branch (definitions in the design doc's Policy model section): allowlist (descriptor-derived at bounded index, scriptPubKey equality; OP_RETURN refused; allowlisted wallet's address beyond max_derivation_index refused), fee ≤ max_fee_pct × Σinputs (at/over/under boundary, all classes incl. escape), input ownership (untrusted-hint re-derivation, bounded scan), sighash enforcement (user sig verified against recomputed sighash; non-ALL sighash refused — subsumes output-mutation), PSBT global/input/output consistency.
- Anti-replay (v0.2 semantics): identical commitment resubmitted within expiry → recorded verdict returned (idempotent); expired commitment → COMMITMENT_EXPIRED refusal; coordinator-proposed expiry beyond node's max_commitment_age_secs → refused; RBF replacement (new commitment, same outpoints) → evaluated fresh, NOT blocked by the log.
- Carrier confirmation clock authority: channel-mode Spend-nonce acceptance records immutable `D` from the signed lifetime remaining using the production-fixed, test-pinnable `HotClock`; non-channel mode remains wall-only. `E` is the signed/federation-facing attempt-authority bound; `D` ends node-local logical residency/actionability. Physical records may remain at/after `D` while an owner rules but confer no holder authority. A new holder requires attempt `< E` and `mono_now < D`; wall refusal retries only while `mono_now < D`; at/past `D` the outcome is terminal, opaque, and non-mutating. Fresh duplicates/completed confirmations remain idempotent. At all three receipt clock-refusal sites and through the narrow peer-authenticated, quota-admitted outer-Stale receipt path, an exact live actionable receipt gets fixed 30-second retry while terminal/nonmatching stale input remains `STALE_TIMESTAMP`; one second remains limited to short owner-in-flight/KDF-busy states. The outer-Stale final decision serializes `sign_state → store` across the acceptance-to-claim gap, while a previously unseen valid coordinator signature uses at most q6v's one nonblocking Carrier-KDF attempt outside both locks and is revalidated. Under the current premise `coord_nonces.high_water < E`, receiver correction can succeed only after elapsed time re-enters the latched 300-second channel window before `E`/`D`; retries end at those bounds. Capacity and safe-removal tests assert one existing coordinator-nonce slot, store-only atomic intent+memo retirement through every `prune_store` driver, a nonce-only tombstone after early/completed Carrier retirement, retention until both wall expiry and `D` end, and unchanged candidate expiry. Shape tests pin `HotClock` to a monotonic second distinct from wall `now` and include the deadline rather than masking it; update the stale `SignStateShape` “needs no clock pin” comment. Freshness-event tests cover lower/equal/higher ingress high-water, retained versus evicted diagnostics, cursor behavior, and unrelated watchtower evidence at the queue cap. Mutation controls independently break wall-or-monotonic nonce liveness, exact-`D` retirement, lookup/final exact-`D` authority, held-sender KDF filtering, 30-second retry, and acceptance-to-claim serialization.
- Malformed PSBT to `/sign` → 400 with structured error, no panic.
- PSBT missing or carrying an invalid user signature → refusal (never a node signature on a user-unsigned tx).
- PSBT for a different descriptor than the node's configured vault → refusal.
- Property tests (proptest): no mutated transaction passes an authorization bound to the original. Built in V0-7; three suites, all `dev-dependencies` only:
  - `crates/vault-node/src/prop_mutation.rs` — structural PSBT mutations (input/output insert, drop, value, scriptPubKey, `nSequence`, `nLockTime`, `nVersion`, prevout, witness bytes) on a user-signed spend AND its paired escape, driven through the real `/sign` ingress: every consensus-relevant mutation changes the `commitment_id`; a mutant carried on the original's `coord_sig` is `COORD_AUTH_INVALID`; a mutant the coordinator *re-signs* is still refused, because the user's signature no longer covers the transaction.
  - `crates/vault-proto/tests/prop_commitment_binding.rs` — the same invariant at the encodings: the commitment's canonical encoding is injective (including input/output ORDER), and a coordinator signature never transfers to a different request or a different `wallet_id`.
  - `crates/policy-core/tests/prop_refusal_core.rs` — arbitrary output sets against a fixed vault/escape/hot descriptor set, checked against a complete refusal oracle: unrecognized output ⇒ refused, mixed hot+escape ⇒ `PSBT_INCONSISTENT`, an attacker-controlled output is only ever `Hot` or refused (never `Escape`/`Refresh`), `hot_outflow` is the non-vault/non-escape sum, and the per-tx Hot cap refuses exactly when outflow exceeds it.
- Decoder robustness (proptest in place of cargo-fuzz, which needs a clang/LLVM toolchain this build environment does not have): arbitrary bytes, near-valid bodies (one field replaced, one byte flipped, truncated), and *authenticated* garbage (a correctly channel-signed envelope or correctly coord-signed request whose payload is noise) at every untrusted-input parser — `crates/vault-node/src/prop_decoder.rs` for the `/channel` envelope verify/decode path and the `/sign` ingress, `crates/vault-proto/tests/prop_decoder_robustness.rs` for the wire DTOs. No input panics; malformed input is a typed refusal; the near-valid corpus asserts the EXACT reject reason so a check that stopped firing cannot hide behind "it was rejected anyway".

- **Operator Core view and inventory, live (2026-08-24, bead btc-policy-m3a-core-view-inventory-rha).** The stage-1 inventory reads a real bitcoind through one closed eight-method read-only seam. Its in-process coverage has TWO seams and neither reaches a daemon — the inventory/preflight classes answer a typed `Fake: CoreView` directly, so nothing there decodes a JSON-RPC payload (the only JSON they build is the `getblockchaininfo` identity object the shared validator itself consumes, `inventory.rs`), while the adapter's decoder fixtures replay canned JSON over a local socket (`core_view.rs`) — so its live behaviour needs its own `#[ignore]`d suite and its own `core-view` leg of CI's `check` matrix:

  ```bash
  nix develop -c cargo test --locked -p vault-cli --test core_view -- --ignored --test-threads=1
  ```

  It spawns a regtest daemon with **no `-txindex`** — where a full previous transaction resolves only through the scan's retained height, `getblockhash`, and a block-qualified `getrawtransaction` — plus two offline signets, and proves what neither in-process seam can: Core's own `-8`/`-5` refusals arriving as the two declared absences, a NONEMPTY inventory preparing with its two preflighted maximum finalized sizes and an integer sat/vB rate ceiled from the daemon's own floors, coinbase maturity turning at exactly 100 confirmations, a fresh node's `initialblockdownload` refusing the whole preparation, the cookie being re-read per call under owner-only rules with no credential in any diagnostic, and the default signet daemon's own `signet_challenge` being what the sealed network code means while a custom signet sharing `chain:"signet"` is refused. What it does NOT exercise, and does not claim to: final valued composition, `non_witness_utxo`/`SIGHASH_ALL` attachment, and a real signer are all `btc-policy-m3b-spend-composition-nq8`, so the pair built live here is the FROZEN SKELETON at test-chosen amounts. The seam is DORMANT — no CLI command dispatches to it — and since `btc-policy-http-bounded-ingress-response-qhe` its one HTTP call site is BOUNDED: one 60-second (or 600-second `scantxoutset`) deadline over the whole exchange, a 16-MiB cap on the entire raw response, strict EOF framing, and strict UTF-8 before this parser sees a byte.

- **Deterministic Spend and base-Escape composition (2026-08-25, bead btc-policy-m3b-spend-composition-nq8).** Stacked on the seam above, `compose_spend` turns that prepared view into the frozen `UserAuthorization::Spend`: the EXACT requested amount, mandatory vault change, a one-output base Escape sweeping the same ordered coins with an EMPTY replacement ladder, each shape priced at its own preflighted vsize (the Escape at `primary_rate.max(sealed escape_feerate_floor)`), every product and subtraction checked, every output at or above its own script's `minimal_non_dust()` — which is rust-bitcoin's and Core's DEFAULT dust policy and establishes NOTHING about a node running a custom `-dustrelayfee` — and the sealed coverage percentage satisfied in widened arithmetic where equality passes. Its in-process classes answer the same kind of typed fake Core and prove that it attaches every verified full previous transaction and an EXPLICIT `SIGHASH_ALL` to EVERY input map of BOTH transactions, invents no BIP32 origin, requires `policy_core::evaluate` — the sole `MAX_FEE_PERCENT` authority, applied to both transactions — plus a DERIVED Hot primary and Escape mandatory leg, and then hands the result to the REAL `SoftwareSigner`, which accepts it and returns exactly two signed members.

  Its LIVE case is the seventh in the same `#[ignore]`d `core-view` leg: against a real no-`-txindex` regtest daemon it composes real values off live chain state, carries byte-for-byte the full parent Core itself returned, signs through the real `SoftwareSigner`, proves both derived classes and the exact amounts, and then proves Core's own UTXO set and mempool are unchanged — nothing here broadcasts. The six earlier live classes are preserved unchanged and still claim none of this work.

  Both halves live in `crates/vault-cli/tests/core_view.rs` by deliberate choice rather than by necessity: every new M3b test is kept under `tests/`, which is what keeps it outside the production-only line budget, and that one target exercises the composer, the inventory, the sealed vault and the real `SoftwareSigner` together — an in-crate `#[cfg(test)]` module in `compose.rs` could reach the same items, on the wrong side of that budget, which is why it carries none. The composer is DORMANT: no CLI command dispatches to it and nothing in it constructs a `CoreRpc`; the production edges that build or attach the pair are `compose_spend -> prepare_view`, `compose_spend -> pair` and the completed-inventory full-parent accessor, while its other M3a edges read or validate the prepared view. The transport under it is bounded and strict since `btc-policy-http-bounded-ingress-response-qhe`; what that 16 MiB bounds is the RAW WIRE, not what decoding it then costs in heap, which is `btc-policy-yw4`.

- **Bounded ingress and Core HTTP responses (2026-08-26, bead btc-policy-http-bounded-ingress-response-qhe).** One `Policy` in `crates/vault-cli/src/http.rs` splits the coordinator's client in two, and both halves are pinned in `http::tests`. `Legacy` is byte-identical to what every pre-M3 caller has always run — fixed connect, per-READ inactivity timeout, unbounded read to close, lossy `String` body, and both of its distinct malformed-response diagnostics — and readiness, the demo, the adversary harness and the signet faucet stay on it deliberately: `NodeProcess::is_serving` polls a node that is still coming up, and a whole-exchange deadline would refuse a slow but shaped projection (`fed::tests::the_readiness_probe_still_reads_a_slow_node_to_close`). The BOUNDED constructor-selected policies — ingress at the caller's deadline and 64 KiB, ordinary Core at 60 seconds, and the single typed `scantxoutset` at 600 seconds, both with 16 MiB — create ONE monotonic deadline BEFORE connect, recompute what is left of it before every connect/write/read and once more AT the terminal EOF (a rounded socket timeout plus a descheduled thread can otherwise deliver EOF past the deadline), cap the ENTIRE raw response in one exactly-`cap+1` zeroizing allocation that is asserted never to move, and complete only at EOF inside both bounds. An injected clock is what makes the deadline rows real: a budget silently restarted per phase finishes on time under any sleep a test could write. `NotSent` is a pre-connect answer and nothing else, and the write loop's every arm — including a write reporting NO progress and an `EINTR` restart, neither of which a real `TcpStream` produces — is driven through the private `Wire` seam, because that one classification is the difference between a bounded-denial bug and a double-submission bug. Framing at EOF is strict: `HTTP/1.0`/`HTTP/1.1` plus three digits in `100..=599`, every header line `token ":" OWS value`, every line in the head ended with CRLF and never with a lone LF or a lone CR — either of which would otherwise leave the first CRLF sitting AFTER a line and hide it, a `Transfer-Encoding` or a disagreeing `Content-Length` above all, from every rule beside it — no `Transfer-Encoding`, at most one `Content-Length` matched ASCII-case-insensitively and equal to the body received, and a strictly valid status survives every later failure with `body: None`. Two REAL-server controls measure rather than assume what that parser depends on: `vault-node`'s own axum `/sign` surface (`the_sign_surface_answers_bytes_the_bounded_ingress_client_can_frame`, inside `cargo test --workspace`) and a real regtest bitcoind (`LIVE-8`, the eighth class in the same `#[ignore]`d `core-view` leg above, so no new CI command is needed).

## Critical Paths
- `btc-vault demo theft-refused` exits successfully from a clean clone with one command (v0.1 success criterion).
- policy-core evaluation is deterministic and pure: same PSBT + policy → same verdict on every node (federation consistency depends on it).
- ~~The seven v1 attack scenarios (stolen-user-key, compromised-node, mutation, replay, stolen-recovery-keys, compromised-quorum, recovery-drill)~~ — **superseded 2026-07-21**, see the banner. The launch-gate set is now `btc-vault attack [scenario|all]`, a live-regtest adversarial harness over a co-located `n = 2t−1` federation with a real compromised minority (`crates/vault-cli/src/attack.rs`). It exits 0 only when every safety assertion holds, and prints a scorecard row per scenario:

  | scenario | what it demonstrates |
  | --- | --- |
  | `arm-split-closed` | the **coupling**: a coordinator splitting the carrier from the signature via each ADR-0012 vector (corrupt escape, oversize past `max_msg_bytes`, lapsing expiry) still leaves ≤ `t−1` releasable partials |
  | `censorship-residual-bounded` | the **Hot budget** (ADR-0014): over-cap refused pre-signing at every node, window sum capped, unexposed reservations returned |
  | `selective-delivery` | one coordinator delivery fans out to every live honest node; the coupling, not an armed-node count, keeps coerced partials frozen, with `wiretap_positive_control` supplying the per-node release-path control and the sweep a second one for the escape-role decoder |
  | `two-spend-probe` | **silence**: identical body, size, and `/events` under both pins, including the Hot-budget refusal path; wall-clock skew is measured and reported as advisory because CI variance exceeds the one-Argon2 effect it was meant to detect. The hard ingress-work gate is deterministic: `normal_and_duress_ingress_op_sequences_*` compares each pin's `/sign` acknowledgement field-for-field, its ordered handler ops, a canonical pin-masked projection of the whole channel store, a projection of the whole `/sign` handler state (replay, pending, coordinator-nonce and refresh logs plus the PIN attempt budget), the channel schedule-work delta, PIN evaluations, and carrier derivations across first-carrier, armed, fresh and replay-cached escape-class, replay-cached Hot, Hot-budget-refusal, and LOCKED-OUT shapes — the last being the attacker's cheapest probe (flood wrong pins, then submit a valid one) and the path where §0's fail-closed rule makes a refused duress request still derive its carrier, run the arm hook, and stage for peers. The two projections are read off fields rather than hand-placed instrumentation, so a duress-only NET state difference anywhere in the channel store or in `SignState` — the latter being where the pin verdict itself is written, via `AttemptBudget::charge` — is covered generically; the vault-authorized set, the peer OUTBOX and the remaining handler locals have only the ordered log's instrumented call-site coverage, as does a write reverted within the same pass (the Hot-budget reserve/unwind pair, which is instrumented for exactly that reason). For the outbox that is `OutboxPush`'s presence and position, never its CONTENTS — the staged request carries the coordinator's bytes with the pin in them, so it differs by pin by construction and would need masking like a carrier id. The capture ends when the synchronous handler returns: the server's subsequent outbox drain and spawned peer sends are exercised by live propagation scenarios but are outside this deterministic record, so it is not an end-to-end timing gate. The `/healthz` and `/pending` duress-oracle properties — byte-identical projection armed-pre-`T` vs idle, and byte-identical under both pins — are proven separately by `healthz_is_byte_identical_on_an_armed_pre_t_node`, `pending_is_byte_identical_on_an_armed_pre_t_node`, and `pending_is_byte_identical_under_the_normal_and_duress_pins`; `demo theft-refused` covers `/pending` end-to-end on the path that matters by requiring every node to name the attacker's accepted candidate. |
  | `toxic-parent` | an escape over a replaced external parent never releases a share; Lockdown at T regardless |
  | `in-flight-refresh` | the escape chains off the refresh output; coarse subordination; coerced spend dead |
  | `escape-class-sequences` | the two-slot record and `Idle→Armed` suppression; the normal pin's delayed slot is a no-op |
  | `hold-expiry-race`, `lockout-then-duress`, `duress-resubmission` | arming against a pending, locked-out, or already-registered commitment |
  | `reboot-death` | a killed node cannot restart or rejoin (ADR-0007); above `t` still sweeps, below `t` is lockdown-only |
  | `fire-time-failure` | coverage / feerate / package refusals stop the sweep but never the unconditional Lockdown |
  | `reorg-watchtower-cursor` | (9y5.3-a) a spend a reorg re-lands AT/BELOW the scan cursor is still classified — the old bare monotonic cursor silently missed it; the reorg-aware one detects the fork, rewinds, and re-scans |
  | `reorg-escape-resettles` | (9y5.3-ii) an armed escape whose confirming block is re-orged out is not stranded — it re-settles from the node's own retained Firing job, and still beats the coerced spend whose input the reorg re-opened |
  | `reorg-duress-lockdown` | (9y5.3-iii) the safety track is indifferent to the chain re-orging under it: Lockdown at `T` stays unconditional across two reorgs, no honest node releases the coerced partial |
  | `recovery` | the demonstrated exit from the lockdown-only branch: V0-10 timelocked recovery spend confirms |

  Each scenario asserts no theft path and — with ONE exception, named because a mutation test caught this sentence overstating itself — the safety track (freeze + unconditional Lockdown at T). The exception is `reorg-watchtower-cursor`, which asserts watchtower re-classification across a reorg and does not assert Lockdown at all: under `btc-policy-nia`'s injected Lockdown-at-T fault it was the single scenario of sixteen that stayed green. Read "the safety track holds across the harness" as fifteen of sixteen scenarios, not all of them. And — where it argues from an ABSENT partial — pairs that absence with a positive control in the same federation, so a deaf wiretap cannot pass vacuously. Two controls do different jobs and neither substitutes for the other. The scenario's own escape sweep is a DECODER control: it shows the listener parses an escape-role envelope, and it is deliberately taken *after* the absence read, so the same decode path is live at a later instant than the zero was read at. But one observed sender cannot license a claim that ranges over every honest node — a single node's silently broken fan-out reads exactly like that node's freeze holding — so any scenario whose headline claim is per-node also runs `wiretap_positive_control` first, while the federation is whole, which requires a validated share from *every* honest daemon. The one exception is `in-flight-refresh`, where a completed control spend keeps a candidate registered and subordinates the very refresh under test (`REFRESH_SUBORDINATED`); it carries the decoder control only, and says so at the call site. Wrapped for CI-style invocation by the four `#[ignore]`d tests in `crates/vault-cli/tests/e2e.rs` (the three demos plus the harness — there is no single umbrella test; CI invokes the four commands directly), and **scheduled on every push** as the `launch-gate` job in `.github/workflows/ci.yml` — `attack all` plus all three demos (first-light, theft-refused, recovery-drill), with the resolved toolchain, binary hashes, effective config, and scorecard uploaded as artifacts. "Scheduled", not "all four always execute": the gate steps deliberately carry no `if: always()`, so the first one to fail skips the rest, and the artifact reports per section which ran. That is not a caveat about the wording — it is the mechanism that made `btc-policy-9yf`'s negative control stop at `demo first-light` and never reach `attack all`, which is why the harness was not mutation-tested by `9yf` and needed a separate control. It has since had one: `btc-policy-nia` (2026-08-10) injected two safety regressions, and the harness caught one and missed the other — the miss being an EXTRACTION path — a spend that completes instantly under the duress PIN, and extraction only WITH STOLEN HOT KEYS, since its outputs pay the user's own hot wallet — which `cargo test` caught instead and which all three demos also cleared. So it CAN retract a green scorecard, and its coverage is uneven in a way that matters. `docs/adr/0017-one-external-review-at-stage-9.md` owns the measurement and every figure; `btc-policy-u98` owns the missing scenario. No numbers here on purpose — the earlier copy of this sentence went stale exactly that way, and a version of it carried the figures while telling the reader they lived elsewhere. A green job means all four passed; a red one means everything up to the failure ran. That job runs the DEBUG build, deliberately, and the workflow header states why and what residual that leaves. That job is the reason "every scenario holds" is a standing property rather than a manual claim; an unenforced gate rots, and for custody code it rots silently — which is not a hypothetical here: the launch gate ran red for dozens of consecutive runs, about half of them dying at startup with **zero steps recorded**, which is why nobody noticed. The exact window, count and split are stated once, in `.github/workflows/ci.yml`'s `workflow_dispatch` comment, which owns that figure — deliberately not restated here, because the earlier copy in this file drifted (it claimed "every run in the retained history", which is false) and that drift is the reason the figure has a single owner at all. The bead filed this as "44/44 … each finishing in 2-12 seconds"; that quantifier is superseded by the measurement and is corrected at `.github/workflows/ci.yml`, which owns this history. Not all of them were stepless — the window opens with a run that executed for the full gate duration and failed — so "all red" and "none ran" are different claims and only the first holds across the whole window. The failure stayed invisible because nobody had ever watched the gate go red (bead `btc-policy-9yf`). `btc-vault demo first-light` remains the one-command smoke path, `btc-vault demo theft-refused` is the named v0 acceptance artifact, and `btc-vault demo recovery-drill` is the V0-10 recovery exit — the `recovery` scenario above overlaps it, but the named demo is now gated in its own right rather than only by proxy.
